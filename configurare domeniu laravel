sudo nano /etc/hosts			#adaugi host-ul

cd /etc/apache2/sites-available
cp app1.com.conf app2.com.conf 	# copiezi conf de la app1 deja facut si il schimbi pt app2
sudo nano app2.com.conf      		# editezi/schimbi tot ce trebe
sudo a2ensite app2.com.conf 		# enable
sudo systemctl reload apache2		#reload apache 2
cd /home/eli/Sites/app2		# mergem pe path-ul care trebe
sudo chmod 777 storage -fR		# schimbam drepturile pt storage


in ~/Sites/links$ 
ls -alt
nano .env				#schimbam datele pt DB


Note: the node command changed location and the old location may be remembered in your current shell.
         old : /usr/bin/node
         new : /usr/local/bin/node
To reset the command location hash either start a new shell, or execute PATH="$PATH"


https://www.technhit.in/setup-laravel-on-ubuntu-with-apache/


ANGULAR

//Restarting the server
Go to the location then:
python3 -m http.server 


use: dan@reea.net
pass: !Reea2020.

In /Eli/Studiu/Angular/firstProject
$ rackup

GIT

Initialize GIT:
git init

Remove it:
rm -rf .git

adding gitignore file:
echo "*.py[cod]
.DS_Store
__pycache__/
*.py[cod]
*$py.class
" > .gitignore


Add file:
git add file
git commit -m "scrie ce ai facut"

Push to repository:
git push orgin master

